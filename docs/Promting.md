# AI Coding 提示词 TODO List

## 需要注意的后续计划
1. Git 提交的格式需求：
  - 模仿 上游：wxj881027/QmClient 的一次提交的内容格式，如：

```
FEAT: 添加显示丢包率功能并优化投票设置

## DEL
- 取消丢包率显示对 `cl_showpred` 的依赖，现在预测时间和丢包率可以分开控制。
- 移除 TC 设置里“只能自动拒绝换图”的单一模式限制。
## FEAT
- 新增独立开关 `cl_show_packet_loss`，用于单独控制游戏内丢包率显示。
- TC 界面的自动换图投票改为三段模式：`Off / Agree / Reject`。
- `tc_auto_vote_when_far` 扩展为模式配置：0 关闭，1 自动拒绝，2 自动同意
- 赞助名单新增
- 版本号更新至 2.61.16。
## FIX
- 修复举报扫描请求在 `HTTP` 失败或取消时读取状态码导致断言的问题。
- 修复举报功能可在 `Axiom` 服务器内使用的问题：入口按钮会禁用，逻辑层也会拦截。
- 修复丢包率独立显示后与右上角消息、冻结 `HUD` 布局避让不一致的问题。
- 引入 `CBackgroundParticles` 组件，用于管理和渲染背景粒子。
- 实现了粒子生成、更新和渲染逻辑，并支持自定义大小、速度和颜色等属性。
- 在设置菜单中添加了粒子数量、深度和光晕效果的配置选项。
- 增强了队伍切换功能，支持在快速练习模式下使用观战命令。
- 更新了皮肤随机生成机制，支持根据所选皮肤使用自定义颜色。
- 改进了移动方块的逻辑，使其能够识别水域图像以实现动态交互。
- 重构了现有代码，以提高可读性和可维护性。
- 修正部分 UI 控件 ID，避免滑条和数值输入冲突。 ）
```

2. 需要 rebase 上游：wxj881027/QmClient 的最近的提交，范围是：从 b27f9b15328b799d198106a972b111672b80289a 到 597893411ea65e4bc3bf2e96849499e34d45aa28
合并上游的时候需要注意，本地的项目已经分裂许久，可能有很多的冲突，需要考虑其作用，按照优先级进行手动合并，也就是，先合并无冲突的，有冲突的地方需要一个一个解决。如何解决？可以区分几个地方：
- 上游新增的功能
- 上游改动过的文档体系，这个 需要先告知我，因为 文档体系相关的，我已经做了一个比较好的体系修改了，而上游是不知道的，所以合并需要小心，不能回退我的操作啥的，应该以更优的文档体系为目标进行整合合并
- UI 相关的，这个我之前合并了部分上游的UI 改动 也进行了我的修改，所以rebase 可能是比较合理的，然后我再进行一个修改
- 皮肤列表、资源页面的性能资源调度的优化，这个是我核心的改动，改动比较大，而上游可能稍有涉及，需要斟酌其架构是否合乎我想要的
总体而言，合并时可以开一个新的专门的合并的分支

然后 看看是否能够提起 PR 到上游，需要进行几个 并行子代理的只读的代码审查，以不同视角进行审查，可以使用 codegraph MCP 进行调查，然后进行修复。

3. ~~文档体系的优化~~：已完成。`.ai/` 已合并到 `docs/ai-workflow/`，`docs/superpowers/` 已取消 git 忽略，`docs/dyl/` 的忽略规则已精确化（只忽略 `BestClient/` 第三方源码），`CHANGELOG_QmClient.md` 已提升到 `docs/` 根目录，`AGENTS.md` 已精简（移除脚本命令细节，补充构建命令），门禁脚本和引用路径已同步更新。

4. 回到本次 PR 的主需求：性能和 UI 的优化，终极目标 = 设置页面中切换 tab 和 子 tab 时，不再阻塞和卡顿，量化的指标应该是（抛砖引玉）：帧时间 <= 显示器的帧率对应的帧时间，这应该是看着不卡顿（无感知）的最低要求吧，其他的要求（需要使用 context7 和 web search 进行一个调研，使用 /code-explore，然后把报告落到 docs\superpowers\explore 下），先定好最终的这个目标，然后再来规划需要实现的新的 UI 渲染的性能优化架构（docs\superpowers\specs\2026-05-24-设置页虚拟化C方案简述.md、docs\superpowers\specs\2026-05-23-设置页预热与FBO缓存设计.md 这两个是之前的做了一半左右的设计吧，但是我认为 最终还是没做完整或者规划没做好，目前的状态还是性能提高微乎其微，需要用第一性原理思考一下性能优化是什么，该如何做，如何适应本游戏 DDNet 原本的 CUI 和 QmUI 框架）（docs\superpowers\specs\2026-05-27-菜单-ui-统一设计.md、这个是 UI 的优化，但是，在具体的实现上面，有很多疏漏，这也是我觉得不够完整和完善的地方，目前的状态上还有很多小的 Bug，我说的 Bug 就是 UI 的视觉表现不够统一以及没有达到我的预期效果，不过对于 UI 而言，经常需要我的指导反馈倒是比较正常的，但是 做的规划，具体落到实现上有很多疏漏就是不应该的）