# 未使用配置项报告

> 生成日期：2026-05-18
> 来源：`qmclient_scripts/check_config_variables.py --qm` 检测结果
> 当前总数：30 项（tclient 22 + qmclient 7 + qmclient_extra 1）

## B 类 — 死配置，建议删除（5 项）

| 配置项 | 来源 | 声明文件 | 原因 |
|-------|------|---------|------|
| `tc_waterfall_enabled` | tclient | config_variables_tclient.h | 已被 `tc_freeze_chat_enabled` 替代，`CheckWaterFall()` 读的是 freeze 系列 |
| `tc_waterfall_emoticon` | tclient | config_variables_tclient.h | 已被 `tc_freeze_chat_emoticon` 替代 |
| `tc_waterfall_message` | tclient | config_variables_tclient.h | 已被 `tc_freeze_chat_message` 替代 |
| `qm_repeat_key` | qmclient | config_variables_qmclient.h | 复读已迁移至绑定系统（`+qm_repeat`），不再读此键值 |
| `qm_pie_menu_key` | qmclient_extra | config_variables_qmclient_extra.h | 饼菜单已迁移至绑定系统（`+pie_menu`），不再读此键值 |

## C 类 — 配置冗余但功能正常（1 项）

| 配置项 | 来源 | 声明文件 | 说明 |
|-------|------|---------|------|
| `tc_regex_chat_ignore` | tclient | config_variables_tclient.h | 通过 Console Chain 回调间接生效（编译到 `m_RegexChatIgnore` 成员），功能正常，检查脚本误报 |

## A 类 — 预留配置，建议保留标注 TODO（24 项）

### tclient sidebar 系列（14 项）

| 配置项 | 功能意图 | 状态 |
|-------|---------|------|
| `tc_sidebar_enable` | 启用侧边栏 | 有菜单声明但渲染逻辑未实现，qmclient 另有独立 sidebar 系统 |
| `tc_sidebar_width` | 侧边栏宽度 | 同上 |
| `tc_sidebar_opacity` | 侧边栏背景不透明度 | 同上 |
| `tc_sidebar_position` | 侧边栏位置（左/右/自定义） | 同上 |
| `tc_sidebar_show_in_game` | 游戏中显示侧边栏 | 同上 |
| `tc_sidebar_show_in_menu` | 在菜单中显示侧边栏 | 同上 |
| `tc_sidebar_show_in_spec` | 旁观模式下显示侧边栏 | 同上 |
| `tc_sidebar_show_fps` | 侧边栏显示 FPS | 同上 |
| `tc_sidebar_show_ping` | 侧边栏显示 ping | 同上 |
| `tc_sidebar_show_speed` | 侧边栏显示玩家速度 | 同上 |
| `tc_sidebar_show_position` | 侧边栏显示玩家位置 | 同上 |
| `tc_sidebar_show_time` | 侧边栏显示本地时间 | 同上 |
| `tc_sidebar_show_server_info` | 侧边栏显示服务器信息 | 同上 |

### JumpHint 子配置（4 项）

| 配置项 | 功能意图 | 状态 |
|-------|---------|------|
| `tc_jump_hint_color` | 跳跃提示颜色 | JumpHint 功能主体已实现（`hud.cpp` 使用 `m_TcJumpHint`），但颜色/位置/大小参数未接入 |
| `tc_jump_hint_x` | 跳跃提示水平位置 | 同上 |
| `tc_jump_hint_y` | 跳跃提示垂直位置 | 同上 |
| `tc_jump_hint_size` | 跳跃提示字体大小 | 同上 |

### 其他（1 项）

| 配置项 | 功能意图 | 状态 |
|-------|---------|------|
| `tc_warlist_reason` | 显示敌对原因 | WarList 功能已实现，nameplates 中无条件显示原因，此开关未被检查 |

### qmclient 动画/语音系列（4 项）

| 配置项 | 功能意图 | 状态 |
|-------|---------|------|
| `qm_scoreboard_anim_optim` | 计分板动画优化 | 全仓库无引用，预留配置 |
| `qm_chat_fade_out_anim` | 聊天框淡出动画 | 全仓库无引用，预留配置 |
| `qm_emoticon_select_anim` | 表情选择动画 | 全仓库无引用，预留配置 |
| `qm_voice_show_ping` | 显示语音延迟 | 语音系统已实现，此显示配置未接入 |
| `qm_voice_show_indicator` | 显示语音指示图标 | 同上 |
| `qm_voice_indicator_above_self` | 显示自己头顶语音指示 | 同上 |

## 行动建议

1. **立即删除 B 类（5 项）** — 纯死代码，无风险
2. **修复 C 类（1 项）** — 让检查脚本识别 Console Chain 回调的间接使用，消除误报
3. **A 类标注 TODO** — 在配置声明处加 `// TODO: 功能待实现` 注释
