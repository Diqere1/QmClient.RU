feature: 2026-05-07-rmlui-render-command-bridge
created: 2026-05-07

steps:
  - action: "graphics-thread callback 最小桥：在 graphics_threaded 命令系统中增加同步 backend callback 命令"
    exit_signal: "主线程可同步请求 graphics thread 执行 callback，且 game-client 构建通过"
    status: done
  - action: "Monitoring HUD 切分：拆分 document render step 和 graph overlay step"
    exit_signal: "RmlUiMonitoringHud 的 document/core render 与 overlay 绘制职责分离"
    status: done
  - action: "宿主接入：RenderRmlUiMonitoringModule 改用 backend-thread frame，不再直接 AcquireBackendFrameContext"
    exit_signal: "Monitoring HUD surface 路径不再出现 AcquireBackendFrameContext/ReleaseBackendFrameContext"
    status: done
  - action: "验证：重建 game-client，并用实际启动日志检查 context 争用路径是否收敛"
    exit_signal: "构建通过，且运行日志不再由 Monitoring HUD surface 直接触发 context acquire 失败"
    status: done

checks:
  - item: "新增的 backend-thread callback 只承接最小必需逻辑，不扩展成通用业务执行器"
    source: 流程级约束
    status: passed
  - item: "Monitoring HUD surface 路径不再调用 AcquireBackendFrameContext/ReleaseBackendFrameContext"
    source: 验收场景
    status: passed
  - item: "runtime/result/fallback 语义保持不变"
    source: 范围守护
    status: passed
  - item: "geometry/scissor/texture 全量 bridge 仍明确标注为未完成"
    source: 范围守护
    status: passed
  - item: "旧 HUD fallback 仍然保留"
    source: 范围守护
    status: passed
